# handles the registry of frameworks and framework version tracking in the database, since this thing serves up executables, the version and framework would be assigned a filepath
# data associated to a framework (from the server perspective)
# - framework name
# - framework version
# - agent_type - (serve the right framework code to the correct platform (mac vs windows))
# - path to framework executable